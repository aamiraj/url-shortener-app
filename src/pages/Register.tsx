import { Link } from "react-router-dom";

const Register = () => {
  return (
    <div>
      <h1>Register page</h1>
      <Link to="/sign-in">Go to sign in page</Link>
    </div>
  );
};

export default Register;
